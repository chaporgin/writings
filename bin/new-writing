#!/usr/bin/env bash
# Create a new article skeleton under content/writings/.
# Usage: ./bin/new-writing "Article title" [--date YYYY-MM-DD] [--slug custom-slug]
set -euo pipefail
. "$(dirname "$0")/_gofind.sh"
exec "$GO_BIN" run . new "$@"
