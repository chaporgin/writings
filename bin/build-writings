#!/usr/bin/env bash
# Validate all articles and regenerate public/ (index, article pages, attachments).
# The build is all-or-nothing: output is only replaced after full validation.
set -euo pipefail
. "$(dirname "$0")/_gofind.sh"
exec "$GO_BIN" run . build "$@"
